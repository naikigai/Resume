# How to build the resume PDF

From the `Resume` folder, run:

```
xelatex -interaction=nonstopmode resume.tex
```

This regenerates `resume.pdf` in the same folder using your existing LaTeX setup (XeLaTeX, with the `fontawesome5` and `roboto` packages already installed).
